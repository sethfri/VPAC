<%= form_for(@publicity_topic) do |f| %>
  <% if @publicity_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@publicity_topic.errors.count, "error") %> prohibited this publicity_topic from being saved:</h2>

      <ul>
      <% @publicity_topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
